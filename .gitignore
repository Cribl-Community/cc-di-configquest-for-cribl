# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build artifacts (the packaged .tgz apps are generated by `npm run package`)
build

# Cribl API spec kept only as a local reference; not a build input (5.5 MB)
openapi.json

# Local agent/session state, if any tooling writes it into the project
.claude/

# Detailed engineer-facing change history — kept locally, never shipped. The official,
# customer-facing changelog is CHANGELOG.md.
CHANGELOG.internal.md